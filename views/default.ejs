<!DOCTYPE html>
<html lang="en"> 
<head>
    <% include partials/head.ejs %>
</head>
<body class="<%= classname %>">
    <div class="container">
        <h1>
            <%= title %>
        </h1>
        <p>
            <%= description %>
        <p>
        <% if( typeof(showfield) !== 'undefined') { %>
            <div class="row">
                <form action="/schema" method="post">
                    <div class="col-lg-12">
                        <div class="form-group">
                            <label for="schema_text">Provied Your Schema:</label>
                            <textarea rows="10" cols="100" id="schema_text" name="schema_feild" class="form-control"></textarea>
                        </div>
                        <button type="submit" class="btn btn-primary pull-right">Submit</button>
                    </div>
                </form>
            </div>
        <% } %>

        <% if( typeof(model) !== 'undefined') { %>
            <div class="row">
                <div class="alert alert-info text-info" style="font-weight:bold; line-height:3rem;">
                    <%= JSON.stringify(model ,null, 4)  %>
                </d iv>
            </div>
        <% } %>
    </div>
    
<footer>
    <% include partials/footer.ejs %>    
</footer>    
</body>
</html>